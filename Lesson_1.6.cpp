// Lesson_1.6
//

#include <iostream>
using namespace std;
#include <Windows.h>
int main() {
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	const int SIZE = 5;
	char s[SIZE][20], * ptr[SIZE];
	cout << "??????? " << SIZE << " ?????:\n";
	for (int i = 0; i < SIZE; i++) {
		cin >> s[i];
		ptr[i] = s[i];//? ?????? ??????? ????? ?????? ??????
	}
	//?????????? ??????? ???????? - ???????????? ?????????, ? ?? ??????
	for (int k = SIZE - 1; k > 0; k--) { //?????? ??????? ??????????? ??????? ???????????
		for (int i = 0; i < k; i++) { //i - ?????? ??????? ???????? ?? ????
			if (*ptr[i] < *ptr[i + 1]) {//????????? ?????? ???? ????
				//???????????? ??????????
				char* tmp = ptr[i];
				ptr[i] = ptr[i + 1];
				ptr[i + 1] = tmp;
			}
		}
	}
	cout << endl;
	//??????? ?????? ? ??????? ??????????
	for (int i = 0; i < SIZE; i++) {
		cout << ptr[i] << endl;
	}
	system("pause");
	return 0;
}
